# 0001. Exclude Task Master

**Status:** Accepted

## Context
`eyaltoledano/claude-task-master` was considered for AI-assisted task planning.

## Decision
Excluded. The native `TaskCreate`/`TaskList`/`TaskGet`/`TaskUpdate`/`TaskOutput`/`TaskStop` tools already provide this functionality, making Task Master redundant.

## Consequences
No gap — native tools cover the need.

## Affects
N/A
