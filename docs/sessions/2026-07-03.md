# 2026-07-03

## oMLX auth + routing observability + menu bar overhaul

- Root-caused local-model selection being broken: winnow's oMLX calls had no
  `Authorization` header, so every request to a non-default model 401'd.
  Added `config.omlx_api_key()` (env override `WINNOW_OMLX_API_KEY`, else
  reads `~/.omlx/settings.json`'s `auth.api_key`) and wired it into both
  `server.py::_call_omlx` and `menu_bar.py`'s `/v1/models` + liveness probes.
  Verified end-to-end against the live server (key confirmed by user: `1234`).
- Killed two stray processes squatting on port 8081 (`mlx_lm.server` PID
  30639 running an unrelated gemma model, plus a hung leftover `curl` test
  client PID 84265). Port confirmed clear.
- `server.py::_detect_omlx_prompt()`: first deleted the silent heuristic
  auto-route branch (short/simple questions routed to oMLX without an
  explicit prefix), then reverted that per user correction — the ask was to
  *improve* it, not remove it. Kept the heuristic, made it observable
  instead: `_detect_omlx_prompt` now returns `(prompt, auto_routed)`, and
  `omlx_response_stream` prepends `[winnow: auto-routed to local oMLX model
  '<id>']` to the response text when `auto_routed` is true, plus reports the
  real active model id instead of a hardcoded `"local-omlx-qwen"` string.
  This is the fix for "claude code never finds out this" — the routing
  decision is now visible directly in the assistant turn content.
- `menu_bar.py`: added a white/dark-adaptive brain icon (SF Symbol
  `brain.head.profile` rendered via PyObjC AppKit to a `template=True` PNG,
  cached at `winnow/assets/menu_icon.png`), a power-state menu item (AC vs.
  battery %, via `pmset -g batt`, refreshed on the existing 5s timer — oMLX
  inference speed is power-state dependent on Apple Silicon), and a
  "Restart oMLX Server" action. The model-select dropdown already existed
  structurally and is now unblocked by the auth fix.
- Restarted everything: oMLX.app relaunched (its own `omlx restart` CLI
  subcommand errored with an IPC/JSON issue talking to the running app —
  worked around via `pkill -x oMLX` + `open -a oMLX`, confirmed healthy on
  port 8000 afterward). Winnow proxy LaunchAgent kickstarted to pick up the
  code changes. Installed the previously-missing
  `com.claude-orchestra.winnow-menu.plist` LaunchAgent (menu bar app had no
  persistent process before this) — first attempt used the script path
  directly and hit `ModuleNotFoundError: No module named 'winnow'` because
  `python3 script.py` only puts the script's own dir on `sys.path`, not the
  repo root; fixed by invoking `-m winnow.menu_bar` instead, matching how
  the proxy's own plist already invokes `-m uvicorn winnow.server:app`.
- Confirmed installed oMLX.app (v0.4.4) already matches the
  jundot/omlx `main` branch the user referenced — no separate install needed.

## Lessons Learned

- When a user says "fix X" and the chosen fix is deleting a behavior, that's
  an interpretation, not a given — confirm what's actually broken (the
  *visibility* of routing) before removing the mechanism itself (the
  routing). Got this wrong once this session and had to revert.
- `python3 /path/to/pkg/module.py` and `python3 -m pkg.module` differ in
  `sys.path[0]`: the former only adds the script's own directory, not the
  package's parent. Any LaunchAgent/cron invocation of an in-package script
  needs `-m`, not a direct file path, or it'll silently `ModuleNotFoundError`
  on any `from pkg import ...` inside it.

## Context budget: why sessions start near 113k tokens (57%) and grow to ~146k after compaction

Measured directly this session (`tiktoken`, `cl100k_base`):
- Global + project `CLAUDE.md` + `MEMORY.md`: ~800 tokens combined — negligible.
- `~/.claude.json` (session/project state file): ~22,200 tokens.
- The base system prompt (identity, tool-use rules, safety rules): a few
  thousand tokens.
- The dominant cost is MCP tool schemas: this session alone has ~15 MCP
  servers connected (arxiv, claude-in-chrome, chrome-devtools, context7,
  github, google-drive, ide, serena, ast-grep, design-sync, remote-trigger,
  cron, etc.), together defining on the order of 90 tool functions, several
  with large verbose JSON schemas (Bash, Monitor, DesignSync, the GitHub
  tool family, browser tools). That schema payload is resent as context on
  every turn and is what actually explains ~113k of the ~200k window —
  CLAUDE.md/memory are rounding errors by comparison.
- Post-compaction the number goes *up*, not down, because compaction only
  replaces old raw turns with a shorter summary — it does not shrink the
  fixed tool-schema baseline (~90-95k) which is always present. On top of
  that baseline: the summary text itself costs tokens (a few thousand for a
  detailed one), and every tool call/result made since resuming (file reads,
  bash output, edits) keeps accumulating until the next compaction. 113k
  (fresh) → 146k (mid-session, post-compaction) is consistent with baseline
  + summary + this session's own resumed work, not a leak or bug.
- Actionable lever: the tool-schema tax scales with *how many MCP servers
  are connected*, not with project size. Disconnecting unused MCP servers
  for a given session is the only way to meaningfully lower the 113k floor.
